apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sketchpad-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sketchpad-service
                port:
                  number: 80
